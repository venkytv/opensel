#
# List of patterns and plugins which use them
#
# Patterns:
# /<regex>/ <whitespace> <plugin-name>
#
# The <regex> can contain at most one pattern match expression (...)
#
/^http://.*$/ browser
/^https://.*$/ browser
/^ftp://.*$/ browser
/^www\..*$/ browser
/\b(1Z ?[0-9A-Z]{3} ?[0-9A-Z]{3} ?[0-9A-Z]{2} ?[0-9A-Z]{4} ?[0-9A-Z]{3} ?[0-9A-Z]|[\dT]\d\d\d ?\d\d\d\d ?\d\d\d)\b/i ups
/\brfc\s+(\d+)\b/i rfc

# Catch-all
/.*/    google
